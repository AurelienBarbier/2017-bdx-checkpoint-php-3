{% extends 'base.html.twig' %}

{% block body %}
    <h1>tvShow</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tvShow.id }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ tvShow.name }}</td>
            </tr>
            <tr>
                <th>Manufacturer</th>
                <td>{{ tvShow.type }}</td>
            </tr>
            <tr>
                <th>Cruisespeed</th>
                <td>{{ tvShow.url }}</td>
            </tr>
            <tr>
                <th>Nbseats</th>
                <td>{{ tvShow.year }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tvshow_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tvshow_edit', { 'id': tvShow.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
